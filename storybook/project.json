{
  "name": "openp41ge-storybook",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "storybook": {
      "executor": "nx:run-commands",
      "options": {
        "command": "storybook dev -p 6006",
        "cwd": "{projectRoot}"
      }
    },
    "build:storybook": {
      "executor": "nx:run-commands",
      "options": {
        "command": "storybook build",
        "cwd": "{projectRoot}"
      }
    }
  }
}
