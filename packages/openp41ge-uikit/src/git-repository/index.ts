/**
 * Git repository browser — UI renderer and shared types.
 * Merged from openp41ge-git-repository.
 */

export { gitBrowserRenderer } from "./git-browser-renderer";
export type {
  GitBrowserData,
  GitBrowserCallbacks,
  BranchEntry,
  CommitEntry,
  DiffStatEntry,
} from "./types";
